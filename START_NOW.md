# 🚀 Быстрый запуск прямо сейчас

## ✅ Что уже сделано:
1. ✅ Создан `.env` файл
2. ✅ Сгенерирован Prisma Client
3. ✅ Клиент запускается на http://localhost:3000

## ⚠️ Важно: Для полной работы нужны PostgreSQL и Redis

### Вариант A: Установить Docker Desktop (Самый простой)

1. Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop/
2. Установите и запустите
3. Затем выполните:
   ```bash
   # Запустить только БД и Redis
   docker-compose up -d postgres redis
   
   # Подождать 10 секунд, затем:
   cd apps/api
   npm run prisma:migrate:dev
   npm run prisma:seed
   
   # Запустить API (в новом терминале)
   npm run start:dev
   ```

### Вариант B: Установить PostgreSQL и Redis локально

**PostgreSQL:**
- Скачайте: https://www.postgresql.org/download/windows/
- Или через Chocolatey: `choco install postgresql`

**Redis:**
- Скачайте: https://github.com/microsoftarchive/redis/releases
- Или используйте WSL

Затем создайте базу:
```sql
CREATE DATABASE dior;
CREATE USER user WITH PASSWORD 'password';
GRANT ALL PRIVILEGES ON DATABASE dior TO user;
```

## 📍 Текущий статус:

- ✅ **Frontend**: Запущен на http://localhost:3000
- ❌ **Backend**: Не запущен (нужна база данных)
- ❌ **База данных**: Не подключена

## 🎯 Что можно сделать сейчас:

1. **Посмотреть интерфейс**: Откройте http://localhost:3000
   - Страницы будут открываться
   - Но API запросы не будут работать без базы

2. **Установить Docker** и запустить базу данных (рекомендуется)

3. **Или установить PostgreSQL/Redis локально**

## 🔧 Команды для запуска API (после настройки БД):

```bash
# В корне проекта
cd apps/api

# Запустить API
npm run start:dev
```

API будет доступен на: http://localhost:3001

## 📝 Что работает без базы:

- ✅ Интерфейс регистрации/входа
- ✅ Страницы Dashboard, Servers, Admin
- ❌ Регистрация/вход (нужен API)
- ❌ Создание серверов (нужен API)
- ❌ Все функции, требующие базу данных

---

**Рекомендация**: Установите Docker Desktop - это самый быстрый способ запустить все необходимое!

