# 🖼️ Оптимизация изображений

## Обнаруженные большие изображения

В проекте найдены следующие изображения, требующие оптимизации:

- `assets/back.png` - ~1.8 MB
- `assets/back_2.png` - ~1.7 MB

## Рекомендации по оптимизации

### 1. Использование Next.js Image Component

Все изображения должны использовать компонент `next/image` для автоматической оптимизации:

```tsx
import Image from 'next/image';

<Image
  src="/assets/back.png"
  alt="Background"
  width={1920}
  height={1080}
  quality={85}
  priority={false}
  placeholder="blur"
/>
```

### 2. Конвертация в WebP/AVIF

Рекомендуется конвертировать PNG/JPG в современные форматы:

**WebP** - уменьшает размер на 25-35% при том же качестве
**AVIF** - уменьшает размер на 50% при том же качестве (лучшая поддержка в Next.js)

### 3. Инструменты для оптимизации

#### Онлайн инструменты:
- [Squoosh](https://squoosh.app/) - лучший инструмент для оптимизации
- [TinyPNG](https://tinypng.com/) - быстрая оптимизация PNG/JPG

#### CLI инструменты:
```bash
# Установка sharp-cli для конвертации
npm install -g sharp-cli

# Конвертация в WebP
sharp -i assets/back.png -o assets/back.webp -q 85

# Конвертация в AVIF
sharp -i assets/back.png -o assets/back.avif -q 50
```

### 4. Автоматическая оптимизация

Next.js автоматически оптимизирует изображения при использовании `next/image`:
- Автоматическая конвертация в WebP/AVIF
- Ресайзинг под разные устройства
- Ленивая загрузка (lazy loading)
- Blur placeholder

### 5. Настройки в next.config.mjs

Оптимизация изображений уже настроена в `next.config.mjs`:
- Форматы: AVIF и WebP
- Размеры устройств оптимизированы
- Кеширование включено

## Текущий статус

✅ Конфигурация Next.js обновлена для оптимизации изображений
⚠️ Большие PNG файлы требуют ручной конвертации в WebP/AVIF
✅ Скрипты оптимизации созданы

## Следующие шаги

1. Конвертировать `back.png` и `back_2.png` в WebP/AVIF
2. Обновить компоненты для использования `next/image`
3. Запустить `npm run build` для проверки оптимизации

