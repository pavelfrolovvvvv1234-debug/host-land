# 🚀 Как запустить локалку

## Быстрый старт

### 1. Откройте терминал и перейдите в папку frontend

```bash
cd C:\Users\xd-user\Desktop\dior\frontend
```

### 2. Установите зависимости (если еще не установлены)

```bash
npm install
```

**Это нужно сделать только один раз!**

### 3. Запустите dev-сервер

```bash
npm run dev
```

### 4. Откройте в браузере

```
http://localhost:3000
```

**Готово! Сайт запущен локально! 🎉**

---

## 📋 Дополнительная информация

### Порты

- **Dev-сервер:** `http://localhost:3000`
- Если порт 3000 занят, Next.js автоматически использует 3001, 3002 и т.д.

### Команды

- `npm run dev` - запуск dev-сервера (горячая перезагрузка)
- `npm run build` - сборка для production
- `npm run start` - запуск production-версии (нужна сборка)
- `npm run lint` - проверка кода

### Остановка сервера

Нажмите `Ctrl + C` в терминале, где запущен сервер.

---

## ⚙️ Переменные окружения (опционально)

Если нужны метрики (Google Analytics, Yandex Metrika) локально, создайте файл `.env.local`:

```bash
# В папке frontend создайте .env.local (если его нет)
```

Содержимое (пример):
```env
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
NEXT_PUBLIC_YANDEX_METRIKA_ID=12345678
NEXT_PUBLIC_BING_VERIFICATION=BC18A6B2F824EE5B82836F7B11AE10D0
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-verification-code
```

**Но для простого запуска это не обязательно!**

---

## 🔧 Если что-то не работает

### Ошибка: "port 3000 is already in use"

```bash
# Windows - найдите процесс на порту 3000
netstat -ano | findstr :3000

# Убейте процесс (замените PID на реальный)
taskkill /PID <PID> /F
```

Или просто закройте другое приложение, которое использует порт 3000.

### Ошибка: "module not found"

```bash
# Переустановите зависимости
rm -rf node_modules
npm install
```

### Ошибка: "Cannot find module"

```bash
# Удалите node_modules и переустановите
cd frontend
rm -rf node_modules package-lock.json
npm install
```

---

## ✅ Готово!

Теперь вы можете:
- ✅ Редактировать файлы - изменения появятся автоматически
- ✅ Тестировать изменения локально
- ✅ Работать без влияния на сервер

**Работайте спокойно! 🎉**

