# 🔥 КОНКРЕТНОЕ РЕШЕНИЕ - почему не работает

## Проблема:

1. ✅ `.env.local` создан локально - работает
2. ❌ На production сервере переменные окружения не настроены
3. ❌ Файлы верификации не доступны на production сервере

---

## ✅ ЧТО СДЕЛАТЬ ПРЯМО СЕЙЧАС:

### 1. На сервере - добавьте переменные окружения

**Если используете Docker/docker-compose:**

Найдите где запускается ваше приложение и добавьте переменные.

**Или создайте файл на сервере:**

Если приложение запущено на сервере напрямую (не в Docker):

```bash
# На сервере
cd /path/to/your/app
nano .env.production
```

Добавьте:
```env
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

**Если используете Coolify/панель управления:**

В настройках приложения → Environment Variables добавьте:

```
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

### 2. Убедитесь что файлы верификации на сервере

**Проверьте на сервере:**

```bash
# На production сервере
ls -la /path/to/app/public/

# Должны быть:
# - google3f9d3d2f65137c4d.html
# - yandex_c97b880978746e2b.html
# - BingSiteAuth.xml
```

**Если файлов нет - скопируйте:**

Файлы находятся в `frontend/public/` на вашем компьютере. Скопируйте их на сервер.

### 3. Перезапустите приложение

После добавления переменных окружения перезапустите приложение.

---

## 🚨 Если используете Next.js напрямую на сервере (не Docker):

### Вариант 1: Создать .env.production на сервере

```bash
# На сервере, в папке где запускается приложение
cat > .env.production << 'EOF'
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
EOF
```

### Вариант 2: Экспорт переменных перед запуском

```bash
# На сервере
export NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
export NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768

# Затем запустите приложение
npm start
```

---

## 🐳 Если используете Docker:

### Обновите docker-compose.yml

Добавьте в секцию `environment`:

```yaml
services:
  frontend:
    environment:
      - NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
      - NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

### Или создайте .env файл

Создайте файл `.env` в папке с docker-compose.yml:

```env
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

И обновите docker-compose.yml:

```yaml
services:
  frontend:
    env_file:
      - .env
```

---

## 🔍 Диагностика:

### Проверьте переменные на production:

```bash
# На сервере, где запущено приложение
echo $NEXT_PUBLIC_GA_MEASUREMENT_ID
echo $NEXT_PUBLIC_YANDEX_METRIKA_ID
```

Если ничего не выводится - переменные не установлены!

### Проверьте файлы верификации:

```bash
# На production сервере
curl https://dior.host/google3f9d3d2f65137c4d.html
curl https://dior.host/yandex_c97b880978746e2b.html
curl https://dior.host/BingSiteAuth.xml
```

Если 404 - файлов нет на сервере!

---

## 📋 Краткий чек-лист:

- [ ] Переменные окружения добавлены на production сервере
- [ ] Файлы верификации скопированы на production сервер
- [ ] Приложение перезапущено после изменений
- [ ] Проверены файлы верификации через curl на production
- [ ] Проверены метрики в браузере на production сайте

---

**Главное: `.env.local` работает только локально! На production нужны переменные окружения на сервере!**


