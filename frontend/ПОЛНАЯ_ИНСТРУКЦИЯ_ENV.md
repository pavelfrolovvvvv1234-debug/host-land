# 📝 Полная инструкция по созданию файла .env.local

## Что это такое?

Файл `.env.local` хранит переменные окружения (настройки) для вашего приложения. Этот файл не должен попадать в git (он уже в .gitignore).

## Шаг 1: Перейдите в папку frontend

```bash
cd frontend
```

Или откройте папку `frontend` в проводнике Windows.

## Шаг 2: Создайте файл .env.local

### Вариант А: Через текстовый редактор (Windows)

1. Откройте папку `frontend` в проводнике
2. **Правой кнопкой мыши** → **Создать** → **Текстовый документ**
3. Назовите файл: `.env.local` (с точкой в начале!)
4. **Важно:** Если Windows спрашивает "изменить расширение?" → нажмите **Да**
5. Откройте файл блокнотом или любым редактором

### Вариант Б: Через командную строку (PowerShell)

```powershell
# Откройте PowerShell в папке frontend
cd frontend

# Создайте файл
New-Item -Path ".env.local" -ItemType File -Force
```

### Вариант В: Через VS Code или другой редактор

1. Откройте папку `frontend` в VS Code
2. Нажмите **Ctrl+N** (новый файл)
3. Сохраните как: `.env.local` (File → Save As)

## Шаг 3: Добавьте содержимое в файл

Скопируйте и вставьте **ВСЁ** это содержимое в файл `.env.local`:

```env
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

**ВАЖНО:**
- Каждая переменная на отдельной строке
- НЕ должно быть пробелов вокруг знака `=`
- НЕ должно быть кавычек вокруг значений
- НЕ должно быть лишних пробелов в конце строк

### ✅ Правильный формат:

```env
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

### ❌ Неправильные форматы:

```env
# НЕПРАВИЛЬНО - кавычки не нужны
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-V906RRPH8Z"

# НЕПРАВИЛЬНО - пробелы вокруг =
NEXT_PUBLIC_GA_MEASUREMENT_ID = G-V906RRPH8Z

# НЕПРАВИЛЬНО - всё в одной строке
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8ZNEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

## Шаг 4: Сохраните файл

Нажмите **Ctrl+S** чтобы сохранить файл.

## Шаг 5: Проверьте что файл создан

### В проводнике Windows:

1. Откройте папку `frontend`
2. Включите отображение скрытых файлов:
   - Вкладка **Вид** → поставьте галочку **Скрытые элементы**
3. Должен появиться файл `.env.local`

### Через командную строку:

```bash
cd frontend
dir .env.local
```

Должен показать файл.

### Через PowerShell:

```powershell
cd frontend
Get-ChildItem -Filter ".env.local"
```

## Шаг 6: Перезапустите dev сервер

### Остановите текущий сервер:

Если сервер запущен, нажмите **Ctrl+C** в терминале.

### Запустите заново:

```bash
cd frontend
npm run dev
```

**ВАЖНО:** Переменные окружения загружаются только при старте сервера! После изменения `.env.local` всегда перезапускайте сервер.

## Шаг 7: Проверьте что всё работает

1. Откройте `http://localhost:3000` в браузере
2. Откройте **DevTools** (F12)
3. Вкладка **Network** (Сеть)
4. Обновите страницу (F5)
5. Должны быть запросы к:
   - `www.googletagmanager.com` (Google Analytics)
   - `mc.yandex.ru` (Yandex Metrika)

## Проверка файлов верификации

Откройте в браузере (после запуска сервера):

- `http://localhost:3000/google3f9d3d2f65137c4d.html`
- `http://localhost:3000/yandex_c97b880978746e2b.html`
- `http://localhost:3000/BingSiteAuth.xml`

Все три должны открыться и показать содержимое.

## Структура файла .env.local

Ваш файл должен выглядеть так:

```
frontend/
  .env.local          ← ваш файл (скрытый, начинается с точки)
  package.json
  next.config.mjs
  ...
```

## Решение проблем

### Проблема: Файл не создаётся

**Решение:**
- Убедитесь что вы в папке `frontend`
- Попробуйте создать через командную строку:
  ```bash
  cd frontend
  echo NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z > .env.local
  echo NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768 >> .env.local
  ```

### Проблема: Переменные не загружаются

**Решение:**
1. Убедитесь что файл называется именно `.env.local` (с точкой!)
2. Перезапустите dev сервер
3. Проверьте что нет опечаток в именах переменных

### Проблема: Windows не показывает файл

**Решение:**
- Включите отображение скрытых файлов (Вид → Скрытые элементы)
- Или используйте команду в PowerShell:
  ```powershell
  Get-ChildItem -Force
  ```

## Пример полного файла .env.local

```env
# Google Analytics 4
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z

# Yandex Metrika
NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768

# Опционально: Google Search Console (если нужен meta tag)
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-code

# Опционально: Bing Webmaster (если нужен meta tag)
# NEXT_PUBLIC_BING_VERIFICATION=your-code
```

## Где находятся файлы верификации?

Файлы верификации уже добавлены в:

```
frontend/public/
  ├── google3f9d3d2f65137c4d.html
  ├── yandex_c97b880978746e2b.html
  └── BingSiteAuth.xml
```

Они будут доступны по адресам:
- `https://dior.host/google3f9d3d2f65137c4d.html`
- `https://dior.host/yandex_c97b880978746e2b.html`
- `https://dior.host/BingSiteAuth.xml`

---

**Готово! После создания файла и перезапуска сервера всё заработает! 🎉**


