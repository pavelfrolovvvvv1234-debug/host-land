# ⚡ Быстрый деплой

## 🚀 Варианты деплоя

### 1. Cloudflare Pages (Рекомендуется - бесплатно)

**Самый простой способ:**
1. Залейте код в GitHub
2. Зайдите на [dash.cloudflare.com](https://dash.cloudflare.com)
3. Pages → Create a project → Connect to Git
4. Выберите репозиторий
5. Нажмите Deploy

**Подробная инструкция:** [DEPLOY_CLOUDFLARE.md](./DEPLOY_CLOUDFLARE.md)

**Преимущества:**
- ✅ Бесплатно
- ✅ Автоматический SSL
- ✅ Автоматический деплой из Git
- ✅ CDN встроен
- ✅ DDoS защита

---

### 2. VPS (Полный контроль)

**Быстрый старт:**
```bash
# На сервере
git clone YOUR_REPO /var/www/dior-host
cd /var/www/dior-host/frontend
npm install
npm run build
pm2 start npm --name "dior-host" -- start
```

**Подробная инструкция:** [DEPLOY_VPS.md](./DEPLOY_VPS.md)

**Преимущества:**
- ✅ Полный контроль
- ✅ Можно настроить как угодно
- ✅ Собственный сервер

---

### 3. Docker на VPS

**Быстрый старт:**
```bash
docker-compose up -d
```

**Подробная инструкция:** См. [DEPLOY_VPS.md](./DEPLOY_VPS.md) раздел Docker

---

## 📋 Перед деплоем

### 1. Проверьте код:
```bash
cd frontend
npm install
npm run build  # Должно собраться без ошибок
```

### 2. Подготовьте переменные окружения:

**Для Cloudflare Pages:**
- Добавьте в Environment variables в Cloudflare Dashboard

**Для VPS:**
- Создайте `.env.production` файл

**Минимальные переменные:**
```env
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
NEXT_PUBLIC_FRONTEND_URL=https://dior.host
```

### 3. Убедитесь, что домены настроены:

- `dior.host`
- `diorhost.net`
- `diors.host`
- `d1or.host`
- `d1ior.com`

---

## 🔄 Обновление после деплоя

### Cloudflare Pages:
- Автоматически при push в `main` ветку

### VPS:
```bash
cd /var/www/dior-host/frontend
git pull
npm install
npm run build
pm2 restart dior-host
```

---

## ✅ Проверка после деплоя

1. Откройте сайт на всех доменах
2. Проверьте консоль браузера (F12) на ошибки
3. Проверьте, что кнопка "Log in" работает правильно
4. Проверьте все страницы

---

## 🆘 Нужна помощь?

- **Cloudflare:** [DEPLOY_CLOUDFLARE.md](./DEPLOY_CLOUDFLARE.md)
- **VPS:** [DEPLOY_VPS.md](./DEPLOY_VPS.md)
- **Coolify:** [DEPLOY_COOLIFY.md](./DEPLOY_COOLIFY.md)

---

**Выберите вариант и следуйте инструкциям!** 🎉


