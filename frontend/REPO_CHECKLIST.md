# ✅ Чеклист: Что должно быть в GitHub репозитории

## 📁 Обязательные файлы и папки

### Конфигурационные файлы
- [x] `package.json` - зависимости проекта
- [x] `package-lock.json` - lock-файл зависимостей
- [x] `next.config.mjs` - конфигурация Next.js
- [x] `tsconfig.json` - конфигурация TypeScript
- [x] `tailwind.config.ts` - конфигурация Tailwind CSS
- [x] `postcss.config.mjs` - конфигурация PostCSS
- [x] `.gitignore` - игнорируемые файлы
- [x] `README.md` - документация проекта

### Исходный код
- [x] `app/` - все страницы Next.js (App Router)
- [x] `components/` - React компоненты
- [x] `lib/` - утилиты и хелперы
- [x] `content/` - контентные файлы
- [x] `blog/` - статьи блога (MDX/MD файлы)
- [x] `assets/` - изображения и статические файлы
- [x] `docs/` - PDF документы

### Конфигурационные файлы проекта
- [x] `_cms.ts`, `_config.ts` - конфигурация CMS
- [x] `_data.yml` - данные
- [x] `*.vto` - шаблоны страниц
- [x] `*.yml` - YAML конфигурации (services.yml, search.yml)
- [x] `robots.txt` - правила для поисковых роботов
- [x] `sitemap.xml` - карта сайта
- [x] `*.css` - стили (animations.css, main.css)
- [x] `favicon.ico`, `favicon.png`, `apple-touch-icon.png` - иконки

### Документация
- [x] `README.md` - основная документация
- [x] `DEPLOY.md` - инструкции по деплою
- [x] `QUICK_START.md` - быстрый старт
- [x] `REPO_CHECKLIST.md` - этот файл

### GitHub Actions (опционально)
- [x] `.github/workflows/deploy.yml` - автоматический деплой
- [x] `.github/workflows/deploy-simple.yml` - простой деплой

## 🚫 Что НЕ должно быть в репозитории

### Папки сборки и зависимости
- [ ] `node_modules/` - устанавливается через `npm install`
- [ ] `.next/` - папка сборки Next.js
- [ ] `out/` - папка статического экспорта
- [ ] `_site/` - сгенерированный сайт
- [ ] `_cache/` - кеш

### Секреты и конфигурация
- [ ] `.env` - переменные окружения
- [ ] `.env*.local` - локальные переменные окружения
- [ ] `*.pem` - приватные ключи

### Временные файлы
- [ ] `*.log` - лог-файлы
- [ ] `errors.log` - файлы ошибок
- [ ] `dummy` - временные файлы
- [ ] `hosting-landing/` - временные папки

### Системные файлы
- [ ] `.DS_Store` - macOS
- [ ] `Thumbs.db` - Windows
- [ ] `.vscode/`, `.idea/` - настройки IDE (опционально)

## ✅ Проверка перед коммитом

Перед тем как делать `git add .`, убедитесь:

```bash
# Проверьте, что .gitignore работает
git status

# Должны увидеть только исходники, НЕ node_modules и .next
```

## 📤 Команды для первого деплоя

```bash
# 1. Проверка статуса
git status

# 2. Добавление всех файлов
git add .

# 3. Коммит
git commit -m "Initial commit: готово к деплою"

# 4. Добавление remote (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/host-land.git

# 5. Push
git push -u origin main
```

## 🎯 Итог

После выполнения всех шагов в репозитории будет:
- ✅ Весь исходный код проекта
- ✅ Все конфигурационные файлы
- ✅ Документация
- ✅ GitHub Actions workflows
- ❌ НЕТ node_modules (правильно!)
- ❌ НЕТ .next (правильно!)
- ❌ НЕТ секретов и временных файлов

**Проект готов к деплою!** 🚀

