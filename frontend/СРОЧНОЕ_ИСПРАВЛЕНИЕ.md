# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ - Почему не работает

## Главная проблема:

**`.env.local` работает ТОЛЬКО локально! На production сервере нужны другие настройки!**

---

## ✅ РЕШЕНИЕ - Делайте СЕЙЧАС:

### 1. Создайте файл `.env.production` 

Файл `.env.production` уже создан! Проверьте что в нём есть:

```env
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

### 2. Файлы верификации нужно ЗАДЕПЛОИТЬ

Файлы уже в `frontend/public/`, но их нужно:
- Закоммитить в git
- Запушить на сервер
- Пересобрать приложение

### 3. Настроить переменные на production сервере

**Если используете Docker:**

Добавьте переменные окружения в docker-compose.yml или настройках сервера:

```yaml
environment:
  - NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
  - NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

**Или на сервере напрямую:**

```bash
export NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
export NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

---

## 🔥 Быстрое исправление:

### Шаг 1: Закоммитьте файлы верификации

```bash
cd frontend
git add public/google3f9d3d2f65137c4d.html
git add public/yandex_c97b880978746e2b.html
git add public/BingSiteAuth.xml
git add .env.production
git commit -m "Add verification files and analytics config"
git push
```

### Шаг 2: На сервере - добавьте переменные окружения

**Если используете Coolify/Docker:**

В настройках приложения добавьте Environment Variables:

```
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

**Если используете прямой деплой:**

Создайте файл `.env.production` на сервере или добавьте в настройки окружения.

### Шаг 3: Пересоберите и перезапустите приложение

```bash
# На сервере
docker-compose down
docker-compose build
docker-compose up -d
```

Или в Coolify - нажмите "Rebuild".

---

## 📋 Проверка после деплоя:

1. **Файлы верификации:**
   - `https://dior.host/google3f9d3d2f65137c4d.html` → должна открыться
   - `https://dior.host/yandex_c97b880978746e2b.html` → должна открыться
   - `https://dior.host/BingSiteAuth.xml` → должен открыться

2. **Метрики:**
   - Откройте `https://dior.host`
   - DevTools → Network
   - Должны быть запросы к Google Analytics и Yandex Metrika

---

**Главное: файлы и переменные окружения нужны на PRODUCTION сервере, а не только локально!**


