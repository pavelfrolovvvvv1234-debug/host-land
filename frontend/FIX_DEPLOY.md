# 🔧 Исправление проблем деплоя

## Проблемы которые были исправлены:

### 1. ❌ Ошибка: `Cannot find module 'critters'`
**Причина:** Экспериментальная опция `optimizeCss: true` требует модуль `critters`, который не установлен.

**Решение:** Отключена опция `optimizeCss` в `next.config.mjs` (это экспериментальная функция и может вызывать проблемы).

### 2. ❌ Ошибка: `ENOENT: no such file or directory, open 'prerender-manifest.json'`
**Причина:** Сборка не завершилась успешно из-за первой ошибки, поэтому файлы в `.next` не созданы.

**Решение:** После исправления первой ошибки нужно пересобрать проект.

---

## 📋 Команды для исправления на сервере:

```bash
# 1. Подключитесь к серверу
ssh root@95.182.98.171

# 2. Перейдите в директорию проекта
cd /var/www/dior-host

# 3. Подтяните исправления из GitHub
git pull origin main

# 4. Перейдите в frontend
cd frontend

# 5. Установите зависимости (если нужно)
npm install

# 6. УДАЛИТЕ старую сборку (важно!)
rm -rf .next

# 7. Соберите проект заново
npm run build

# 8. Перезапустите PM2
pm2 restart dior-host

# 9. Проверьте логи
pm2 logs dior-host --lines 50
```

---

## ✅ Проверка что всё работает:

1. **Проверьте что сборка прошла без ошибок:**
   ```bash
   # Должно быть: "✓ Generating static pages (164/164)" без ошибок
   ```

2. **Проверьте что приложение запущено:**
   ```bash
   pm2 status
   # Должно быть: status: online
   ```

3. **Проверьте сайт:**
   - Откройте https://dior.host
   - Все страницы должны открываться
   - Консоль браузера (F12) без критических ошибок

---

## 🚨 Если всё ещё не работает:

### Очистите всё и пересоберите:
```bash
cd /var/www/dior-host/frontend
rm -rf .next node_modules package-lock.json
npm install
npm run build
pm2 restart dior-host
```

### Проверьте переменные окружения:
```bash
# Убедитесь что есть .env.production
cat .env.production
# Должны быть:
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
# NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
```

---

**После выполнения этих команд всё должно заработать!** 🎉
