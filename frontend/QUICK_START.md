# 🚀 Быстрый старт - Деплой на GitHub

## Шаг 1: Подготовка репозитория

```bash
# Проверьте, что вы в правильной папке
cd "C:\Users\xd-user\Desktop\git host"

# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: готово к деплою"
```

## Шаг 2: Создание репозитория на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите **"New repository"** (зеленая кнопка)
3. Название: `host-land`
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)
5. Нажмите **"Create repository"**

## Шаг 3: Push кода в GitHub

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/host-land.git

# Переименуйте ветку в main
git branch -M main

# Отправьте код
git push -u origin main
```

## Шаг 4: Готово! ✅

Теперь весь код проекта находится в GitHub репозитории `host-land`.

### Что дальше?

**Для деплоя на сервер:**
1. Склонируйте репозиторий на сервер: `git clone https://github.com/YOUR_USERNAME/host-land.git`
2. Установите зависимости: `npm install`
3. Соберите проект: `npm run build`
4. Запустите: `npm start`

**Для автоматического деплоя:**
Смотрите файл `DEPLOY.md` для настройки GitHub Actions.

## 📝 Важно!

- ✅ Все исходники проекта в репозитории
- ✅ `node_modules` и `.next` НЕ в репозитории (правильно!)
- ✅ Можно просто клонировать и запустить `npm install`

## ❓ Проблемы?

Если при `git push` возникает ошибка:
- Проверьте, что вы авторизованы в GitHub
- Убедитесь, что репозиторий создан
- Попробуйте использовать SSH вместо HTTPS

