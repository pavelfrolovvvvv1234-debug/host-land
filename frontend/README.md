# Dior Host - Landing Page

Современный лендинг для хостинг-провайдера Dior Host, построенный на Next.js 14.

## 🚀 Структура проекта

Проект состоит из:
- **Фронтенд**: Next.js 14 с TypeScript, React, Tailwind CSS
- **Бекенд API**: Внешний API сервис (api.dior.host/v1)

## 📋 Требования

- Node.js 18+ 
- npm или yarn

## 🛠️ Установка и запуск

### Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Откройте http://localhost:3000 в браузере
```

### Сборка для production

```bash
# Сборка проекта
npm run build

# Запуск production-сервера
npm start
```

## 📁 Структура файлов в репозитории

В репозитории должны быть следующие файлы и папки:

```
host-land/
├── app/                    # Next.js App Router (страницы)
├── components/             # React компоненты
├── lib/                    # Утилиты и хелперы
├── content/                # Контентные файлы
├── blog/                   # Статьи блога (MDX/MD)
├── assets/                 # Изображения и статические файлы
├── docs/                   # PDF документы
├── public/                 # Публичные статические файлы (если есть)
├── package.json            # Зависимости проекта
├── package-lock.json       # Lock-файл зависимостей
├── next.config.mjs         # Конфигурация Next.js
├── tsconfig.json           # Конфигурация TypeScript
├── tailwind.config.ts      # Конфигурация Tailwind CSS
├── postcss.config.mjs      # Конфигурация PostCSS
├── .gitignore              # Игнорируемые файлы Git
├── README.md               # Этот файл
└── .github/                # GitHub Actions workflows (опционально)
    └── workflows/
        └── deploy.yml
```

## 🚫 Что НЕ должно быть в репозитории

Следующие файлы и папки **НЕ должны** попадать в Git:

- `node_modules/` - зависимости (устанавливаются через `npm install`)
- `.next/` - папка сборки Next.js
- `out/` - папка статического экспорта
- `.env` и `.env*.local` - файлы с секретами
- `*.log` - лог-файлы
- `errors.log` - файлы ошибок

## 📤 Деплой через GitHub

### Вариант 1: Ручной деплой

1. **Подготовка репозитория:**
   ```bash
   # Инициализация Git (если еще не сделано)
   git init
   
   # Добавление всех файлов
   git add .
   
   # Коммит
   git commit -m "Initial commit"
   
   # Добавление remote репозитория
   git remote add origin https://github.com/your-username/host-land.git
   
   # Push в GitHub
   git push -u origin main
   ```

2. **Деплой на хостинг:**
   - Склонируйте репозиторий на сервер
   - Установите зависимости: `npm install`
   - Соберите проект: `npm run build`
   - Запустите: `npm start`

### Вариант 2: Автоматический деплой через GitHub Actions

Проект включает GitHub Actions workflow для автоматического деплоя.

**Настройка:**

1. Добавьте секреты в GitHub репозиторий (Settings → Secrets and variables → Actions):
   - `DEPLOY_HOST` - адрес сервера
   - `DEPLOY_USER` - пользователь для SSH
   - `DEPLOY_KEY` - приватный SSH ключ
   - `DEPLOY_PATH` - путь на сервере для деплоя

2. При каждом push в `main` ветку будет автоматически запускаться деплой.

## 🔧 Скрипты

- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка для production
- `npm start` - запуск production-сервера
- `npm run lint` - проверка кода линтером
- `npm test` - запуск тестов

## 📝 Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **Framer Motion** - анимации
- **React Markdown** - рендеринг Markdown

## 🌐 API

Бекенд API находится на внешнем сервере: `https://api.dior.host/v1`

Документация API доступна на странице `/api` сайта.

## 📄 Лицензия

Private - все права защищены.

## 👥 Поддержка

Для вопросов и поддержки обращайтесь к команде разработки.

