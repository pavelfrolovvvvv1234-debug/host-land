# 📦 Инструкция по деплою

## Быстрый старт

### 1. Подготовка репозитория

Убедитесь, что в проекте есть все необходимые файлы:

```bash
# Проверьте, что .gitignore настроен правильно
cat .gitignore

# Проверьте структуру проекта
ls -la
```

### 2. Инициализация Git (если еще не сделано)

```bash
git init
git add .
git commit -m "Initial commit"
```

### 3. Создание репозитория на GitHub

1. Зайдите на [GitHub](https://github.com)
2. Создайте новый репозиторий с именем `host-land`
3. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

### 4. Push кода в GitHub

```bash
# Добавьте remote репозиторий
git remote add origin https://github.com/YOUR_USERNAME/host-land.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Push кода
git push -u origin main
```

## 🚀 Варианты деплоя

### Вариант A: Простой деплой (рекомендуется для начала)

1. **Склонируйте репозиторий на сервер:**
   ```bash
   git clone https://github.com/YOUR_USERNAME/host-land.git
   cd host-land
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Соберите проект:**
   ```bash
   npm run build
   ```

4. **Запустите:**
   ```bash
   npm start
   ```

5. **Или используйте PM2 для постоянной работы:**
   ```bash
   npm install -g pm2
   pm2 start npm --name "dior-host" -- start
   pm2 save
   pm2 startup
   ```

### Вариант B: Автоматический деплой через GitHub Actions

#### Настройка секретов в GitHub:

1. Зайдите в Settings → Secrets and variables → Actions
2. Добавьте следующие секреты:

   - `DEPLOY_HOST` - IP адрес или домен вашего сервера (например: `123.45.67.89`)
   - `DEPLOY_USER` - имя пользователя для SSH (например: `root` или `deploy`)
   - `DEPLOY_KEY` - приватный SSH ключ (содержимое файла `~/.ssh/id_rsa`)
   - `DEPLOY_PATH` - путь на сервере (например: `/var/www/dior-host`)

#### Генерация SSH ключа (если нет):

```bash
# На вашем компьютере
ssh-keygen -t rsa -b 4096 -C "github-actions"

# Скопируйте публичный ключ на сервер
ssh-copy-id user@your-server.com

# Скопируйте приватный ключ в GitHub Secrets (DEPLOY_KEY)
cat ~/.ssh/id_rsa
```

#### После настройки:

При каждом push в ветку `main` будет автоматически запускаться деплой.

### Вариант C: Деплой через Vercel/Netlify

1. **Vercel:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Импортируйте репозиторий GitHub
   - Vercel автоматически определит Next.js и задеплоит

2. **Netlify:**
   - Зайдите на [netlify.com](https://netlify.com)
   - Импортируйте репозиторий GitHub
   - Build command: `npm run build`
   - Publish directory: `.next`

## 🔄 Обновление проекта

### После изменений в коде:

```bash
# На локальной машине
git add .
git commit -m "Описание изменений"
git push origin main
```

### На сервере (если деплой не автоматический):

```bash
cd /path/to/host-land
git pull origin main
npm install
npm run build
pm2 restart dior-host
```

## ✅ Проверка деплоя

После деплоя проверьте:

1. ✅ Сайт открывается: `https://your-domain.com`
2. ✅ Все страницы работают
3. ✅ API документация доступна: `https://your-domain.com/api`
4. ✅ Нет ошибок в консоли браузера
5. ✅ Нет ошибок в логах сервера

## 🐛 Решение проблем

### Ошибка: "Module not found"

```bash
# Удалите node_modules и переустановите
rm -rf node_modules package-lock.json
npm install
```

### Ошибка: "Build failed"

```bash
# Проверьте версию Node.js (нужна 18+)
node --version

# Очистите кеш Next.js
rm -rf .next
npm run build
```

### Ошибка: "Port already in use"

```bash
# Найдите процесс, использующий порт 3000
lsof -i :3000

# Или измените порт в package.json
# "start": "next start -p 3001"
```

## 📝 Важные замечания

1. **Не коммитьте секреты** - используйте переменные окружения
2. **Не коммитьте `node_modules`** - они должны устанавливаться через `npm install`
3. **Не коммитьте `.next`** - это папка сборки
4. **Проверяйте `.gitignore`** - убедитесь, что все ненужные файлы игнорируются

## 🔐 Переменные окружения

Если нужны переменные окружения, создайте файл `.env.local` (он уже в .gitignore):

```env
NEXT_PUBLIC_API_URL=https://api.dior.host/v1
```

**Важно:** Файлы `.env*.local` не должны попадать в Git!

