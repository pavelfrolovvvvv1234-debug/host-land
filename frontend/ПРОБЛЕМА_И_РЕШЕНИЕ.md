# 🚨 Проблема и решение

## Проблема:

1. **Метрики не работают** - переменные окружения не настроены на production
2. **Файлы верификации не читаются** - они не доступны на production сервере

---

## ✅ РЕШЕНИЕ:

### Проблема 1: Метрики не работают

**Причина:** `.env.local` работает ТОЛЬКО локально! На production нужны переменные окружения на сервере.

**Решение:**

#### Если приложение запущено напрямую на сервере:

```bash
# На сервере, где запущено приложение
cd /path/to/your/app
cat > .env.production << 'EOF'
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
EOF

# Перезапустите приложение
pm2 restart all
# или
systemctl restart your-app
```

#### Если используете Docker:

Обновите docker-compose.yml или добавьте переменные в настройках Docker.

#### Если используете Coolify/панель управления:

В настройках приложения → Environment Variables добавьте переменные.

---

### Проблема 2: Файлы верификации не читаются

**Причина:** Google/Yandex/Bing пытаются прочитать файлы с `https://dior.host/...`, но файлы находятся только на вашем компьютере.

**Решение:**

#### Вариант 1: Закоммитить и задеплоить

```bash
# На вашем компьютере
cd frontend
git add public/google3f9d3d2f65137c4d.html
git add public/yandex_c97b880978746e2b.html
git add public/BingSiteAuth.xml
git commit -m "Add verification files"
git push

# После деплоя файлы будут на сервере
```

#### Вариант 2: Скопировать файлы напрямую на сервер

```bash
# На сервере
cd /path/to/your/app/public

# Скопируйте файлы:
# - google3f9d3d2f65137c4d.html
# - yandex_c97b880978746e2b.html
# - BingSiteAuth.xml
```

---

## 🔥 Что сделать СЕЙЧАС:

### 1. На production сервере добавьте переменные

**Узнайте как запущено приложение и добавьте переменные окружения.**

### 2. Убедитесь что файлы верификации на сервере

**Проверьте доступность:**
```bash
curl https://dior.host/google3f9d3d2f65137c4d.html
```

### 3. Перезапустите приложение

**После изменений обязательно перезапустите!**

---

## 📞 Нужна помощь?

Скажите:
1. Как запущено приложение на production? (Docker, PM2, systemd, Coolify?)
2. Где находится папка приложения на сервере?

И я дам точные команды для вашего случая!


