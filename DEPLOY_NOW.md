# 🚀 Быстрый деплой на VPS

## Вариант 1: Через SSH (если есть доступ)

Подключитесь к серверу и выполните:

```bash
# Подключитесь к серверу
ssh root@95.182.98.171

# Перейдите в директорию проекта
cd /var/www/dior-host

# Подтяните изменения из GitHub
git pull origin main

# Перейдите в frontend
cd frontend

# Установите зависимости (если нужно)
npm install

# Соберите проект
npm run build

# Перезапустите приложение
pm2 restart dior-host
# или если PM2 не запущен:
pm2 start npm --name "dior-host" -- start
```

## Вариант 2: Если используете Docker

```bash
ssh root@95.182.98.171

cd /var/www/dior-host
git pull origin main
docker-compose down
docker-compose build frontend
docker-compose up -d frontend
```

## Вариант 3: Через панель управления (Coolify/другая)

1. Зайдите в панель управления
2. Найдите проект "dior-host"
3. Нажмите "Redeploy" или "Pull & Deploy"
4. Дождитесь завершения

## ⚠️ Важно после деплоя:

1. **Проверьте переменные окружения** на сервере:
   ```bash
   # Убедитесь что есть .env.production с:
   NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V906RRPH8Z
   NEXT_PUBLIC_YANDEX_METRIKA_ID=105570768
   ```

2. **Проверьте логи**:
   ```bash
   pm2 logs dior-host --lines 50
   # или для Docker:
   docker-compose logs --tail=50 frontend
   ```

3. **Проверьте сайт**:
   - Откройте https://dior.host
   - Проверьте консоль браузера (F12)
   - Убедитесь что все страницы открываются

## 🔧 Если что-то пошло не так:

### Проблема: "git pull" требует пароль
```bash
# На сервере настройте SSH ключ для GitHub
ssh-keygen -t ed25519 -C "your_email@example.com"
cat ~/.ssh/id_ed25519.pub
# Добавьте этот ключ в GitHub Settings → SSH Keys
```

### Проблема: PM2 не запускается
```bash
pm2 logs dior-host --lines 100
# Проверьте ошибки в логах
```

### Проблема: Порт занят
```bash
lsof -i :3000
# Убейте процесс или измените порт
```

---

**Выполните команды на сервере и всё будет готово!** 🎉
